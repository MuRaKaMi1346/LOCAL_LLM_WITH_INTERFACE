# .env ใช้เก็บเฉพาะ ngrok token เท่านั้น
# ค่าอื่นทั้งหมด (LINE token, model, RAG ฯลฯ) ตั้งได้ในหน้า Admin → บันทึกลง config.json

# ngrok (optional — ใช้เพื่อ expose webhook ออก internet)
NGROK_AUTH_TOKEN=your_ngrok_auth_token_here
